# Development profile
#
# Runtime credentials and service endpoints are configured through environment
# variables referenced by application.yml. Keep machine-specific overrides out
# of version control.
